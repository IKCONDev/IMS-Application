login:
  url:
    path: /users/login
server:
  port: 8076
  max-http-header-size: 800KB
spring:
  main:
    allow-circular-references: true
  application:
    name: IMS-APPLICATION
  devtools:
    restart:
      enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
token:
  expiration_time: 8640000
  secret: hgsd75767sdj87sd708bjh65

client:
  request-details:
    url: http://ip-api.com/json/

---
spring:
  config:
    activate:
      on-profile: sit
  datasource:
    driver-class-name: org.postgresql.Driver
    password: manager
    url: jdbc:postgresql://localhost:5432/imsdb
    username: postgres
    hikari:
      max-lifetime: 900000
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    password: kllb xamb xsmn psnb
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
    username: umssuite2@gmail.com
      
---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    password: manager
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=imsdb
    username: postgres
    hikari:
      maximum-pool-size: 100
      max-lifetime: 900000
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  mail:
    host: smtp.gmail.com
    password: kllb xamb xsmn psnb
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
    username: umssuite2@gmail.com

  


